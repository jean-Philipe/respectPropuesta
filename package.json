{
  "name": "respect-intern",
  "version": "1.0.0",
  "description": "Plataforma interna de Respect - Prototipo",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install && cd ../netlify/functions && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}

